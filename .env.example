# Render settings
TARGET_FPS=60
STEPS_PER_FRAME=10
SPEED_STEP=5
MAX_SPF=500
MIN_SPF=1

# Grid initialisation
NUM_NOISE_CELLS=200
# Noise radius = min(window_w, window_h) // (CELL_SIZE * NOISE_SPREAD_DIVISOR)
NOISE_SPREAD_DIVISOR=8
RANDOM_DIRECTIONS=true
